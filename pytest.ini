[pytest]
# Operational snapshots are immutable deployment evidence, not source test
# trees.  Excluding them prevents recursive collection and module-name
# collisions when running the backend suite from the repository root.
testpaths = backend/tests
norecursedirs = .git .next node_modules ops/snapshots tmp debug_fb_publish.db
